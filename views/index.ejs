<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Breathing Guide</title>
  <style>
    body {
      font-family: system-ui, sans-serif;
      background-color: #0e0e10;
      color: #f0f0f0;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }

    h1 {
      margin-bottom: 2rem;
      color: #a0d8ef;
    }

    .container {
      position: relative;
      width: 40px;
      height: 300px;
      background: #222;
      border-radius: 20px;
      overflow: hidden;
      box-shadow: 0 0 10px rgba(0,0,0,0.5);
    }

    .line {
      position: absolute;
      left: 0;
      width: 100%;
      height: 4px;
      background-color: #4fc3f7;
      border-radius: 2px;
      animation: breathe 11s ease-in-out infinite;
    }

    @keyframes breathe {
      0% { top: 100%; }
      50% { top: 0%; }
      100% { top: 100%; }
    }

    .instruction {
      margin-top: 1.5rem;
      font-size: 1.2rem;
      opacity: 0.8;
      animation: textChange 11s ease-in-out infinite;
    }

    @keyframes textChange {
      0%, 45% {
        content: "Inhale";
      }
      55%, 100% {
        content: "Exhale";
      }
    }

    /* Use JavaScript to swap text since CSS content doesn’t apply to real text */
  </style>
</head>
<body>
  <h1>Follow Your Breath</h1>
  <div class="container">
    <div class="line" id="line"></div>
  </div>
  <div class="instruction" id="instruction">Inhale</div>

  <script>
    const instruction = document.getElementById('instruction');
    let isInhale = true;

    setInterval(() => {
      isInhale = !isInhale;
      instruction.textContent = isInhale ? 'Inhale' : 'Exhale';
    }, 5500); // change every 5.5 seconds
  </script>
</body>
</html>